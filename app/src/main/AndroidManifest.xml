<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.remart">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera"
        android:required="true" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/remart_square"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/remart"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"></activity>

        <activity
            android:name=".LoginActivity"
            android:label="Login Activity"></activity>

        <activity
            android:name=".ProfileActivity"
            android:label="Profile Activity"></activity>

        <activity
            android:name=".AddListingActivity"
            android:label="Add Listing Activity"></activity>

        <activity
            android:name=".AccountEditActivity"
            android:label="Account Edit Activity"></activity>

        <activity
            android:name=".ListActivity"
            android:label="List Activity"></activity>

        <activity
            android:name=".SignupActivity"
            android:label="Sign up Activity"></activity>

        <activity
            android:name=".ResetPasswordActivity"
            android:label="Reset Password Activity"></activity>

    </application>

</manifest>